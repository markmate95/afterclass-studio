# AGENTS.md

## Cursor Cloud specific instructions

This repository (`afterclass-studio`) is currently an empty project initialized with only a `README.md`. No application code, dependencies, or build configuration exist yet.

### Current state
- **No package manager or lockfile** — no `package.json`, `requirements.txt`, `pyproject.toml`, etc.
- **No services to start** — no dev server, database, or background workers.
- **No tests or linting** — no test framework or linter is configured.
- **No build step** — nothing to compile or bundle.

### For future agents
When source code is added, update this file with:
- How to install dependencies and which package manager to use.
- How to start development services.
- How to run tests and linting.
- Any non-obvious caveats discovered during setup.
